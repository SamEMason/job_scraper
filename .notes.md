# Note

Add retry logic to `reqId` scraping - Add failed jobs to array - Fetch them in batches after

Breakout URL logic into URL class - Base - Filters - Method to get Filters url - url.base && url.filters

EXPERIMENT: - https://careers.toasttab.com/jobs/search?page=1&remote=true&department_uids%5B%5D=546da8e254b79111ee592914ea196336&query= - The next few days, check whether this link works - Does the UUID change with different sessions? - HYPOTHESIS: IT DOES - If I am wrong, I can just use this url hardcoded for pagination

OKAY regarding the filter uids... - If they are fixed, I can get them all once and use them forever - If they expire, I can poll them daily along with the job postings - This way I can just use the filter IDs that are persisted - PAGINATION WILL BE EASY

Let's break this url down:

Base: https://careers.toasttab.com/jobs/search

Search params: ?

Pagination: page=1

Remote filter: &remote=true

Department => Engineering filter: &department_uids%5B%5D=546da8e254b79111ee592914ea196336

Not sure: &query=
/////////////////////////////////////////////////

SO TOMORROW:::

    - Get a generic JSON persistence layer setup
    - Implement Filter.getDepartmentFilterUrls()
        - This will yield a JSON object with key value dept: dept_uid

    {
        ...,

        "engineering": {
            "dept_uid": "546da8e254b79111ee592914ea196336"
            "scraped_at": <timestamp>
        },

        "Finance": {...},

        ...
    }

This can then be used to determine if they expire, are changed, and if so how often.

A scheduler can trigger this process daily, updating the JSON when new uids appear.
Need to figure out a scheme to determine the frequency.

Maybe update_frequency which = Date.now() - scraped_at

Hmm how to track the updates, the previous values, the previous update dates...

It's not super important, but it's an experiment to set up.




( Retry logic:
1. Navigate to the url with the current uid for the filter you want to use
2. If it fails because the uid expired, scrape to update the uids
3. Navigate to the url with the updated uid )
