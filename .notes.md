# Note


Add retry logic to `reqId` scraping
    - Add failed jobs to array
    - Fetch them in batches after

Breakout URL logic into URL class
    - Base
    - Filters
    - Method to get Filters url
    - url.base && url.filters

EXPERIMENT:
    - https://careers.toasttab.com/jobs/search?page=1&remote=true&department_uids%5B%5D=546da8e254b79111ee592914ea196336&query=
    - The next few days, check whether this link works
    - Does the UUID change with different sessions?
    - HYPOTHESIS: IT DOES
    - If I am wrong, I can just use this url hardcoded for pagination

OKAY regarding the filter uids... 
    - If they are fixed, I can get them all once and use them forever
    - If they expire, I can poll them daily along with the job postings
    - This way I can just use the filter IDs that are persisted
    - PAGINATION WILL BE EASY